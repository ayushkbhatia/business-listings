/**
 * The goods storefront overview's sections, in the order `/b/[slug]` draws them.
 *
 * A fixed run, and code rather than configuration: the sector template model
 * that once composed these per trade was cut with boards `5a`–`5c`.
 */
export { Hero } from "./Hero";
export { TrustStrip } from "./TrustStrip";
export { CatalogueGrid } from "./CatalogueGrid";
export { Reviews } from "./Reviews";
export { Branches } from "./Branches";
